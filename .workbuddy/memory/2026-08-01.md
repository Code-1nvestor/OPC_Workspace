# 2026-08-01

## OPC 一人公司工作台 — 项目启动（方案已定，待执行）

- 需求：用 codebuddy-chat-web 模板 + Electron 封装 Win11 桌面工作台
- 计划文件：`C:\Users\24738\.workbuddy\plans\toasty-beacon-turing.md`
- 项目路径：`E:\WorkBuddy_workspace\OPC_Workspace\opc-workbench`（待创建）
- 关键事实：模板后端端口实为 **3000**（vite proxy 同），非 3001
- 用户确认决策：
  - 本版本不做聊天（暂无 API Key），聊天页保留为次要页 + 无 Key 引导
  - 模块：进行中事项 / 待办 / AI 资讯(AIHOT) / 重要日期倒计时 / 常用链接导航 / 番茄钟（未选快捷笔记）
  - 关闭窗口 → 最小化到系统托盘；托盘退出后零资源残留
  - 打包：electron-builder portable 免安装 exe（x64）
- 核心设计：Express 内嵌 Electron 主进程（listen(0) 动态端口）；AIHOT 后端代理硬编码浏览器 UA + SQLite 缓存 10min + stale 降级；前端 useVisiblePolling（Page Visibility 感知轮询）；模块 registry 注册机制；DB 路径用 OPC_DB_PATH 环境变量
- 已交付 18 步开发计划表（5 阶段 / 里程碑 M1-M5），等用户确认后按阶段 0→5 执行

## OPC Workbench 阶段0 脥手架（续）

- 继续上次中断的模板文件复制工作，40 个模板文件全部写入 opc-workbench
- Bash/PowerShell 工具无法执行命令（exit 1 无输出），npm install / git commit 需用户手动操作
- 用户需手动执行：`cd opc-workbench && npm install && npm run dev` 验证 M1，然后 git init + commit
- 阶段0文件层面已完成，依赖安装+M1验证+git commit 待手动操作

## OPC Workbench 阶段0 完成 + 阶段1 完成

- Bash 工具恢复，npm install 成功（97 packages），M1 验证通过（后端 tsx 无报错启动）
- Git commit 72ab299: 阶段0 脚手架（39 files, 11099 insertions）
- 阶段1 后端改造完成（git commit 832595e, 8 files changed, 734 insertions）：
  - server/db.ts: OPC_DB_PATH 环境变量化, closeDb() 导出, 6 张新表 (todos/ongoing_items/countdowns/links/focus_sessions/news_cache), 每表完整 CRUD 函数
  - server/routes/: 6 个路由文件 (todos.ts/ongoing.ts/countdowns.ts/links.ts/news.ts/focus.ts)
  - server/routes/news.ts: AIHOT 代理, 硬编码浏览器 UA, fetch + 10s 超时, SQLite 缓存 TTL 10min, stale 降级
  - server/index.ts: startServer() 导出, OPC_EMBEDDED 静态托管 + SPA fallback, 6 组路由挂载
- 全部 7 组 API 测试通过: health/todos CRUD/ongoing CRUD/countdowns CRUD/links CRUD/focus CRUD/news(7 items from AIHOT, cached=false)

## OPC Workbench 阶段0+1 git 修复 + 阶段2 完成 (2026-08-04)

- 修复 git 问题：opc-workbench 子目录有独立 .git（之前 commit 到了子仓库而非根仓库），已删除子 .git，重新提交到根仓库并 push
- 阶段2 前端改造完成（git commit d64e42f, 23 files changed, 1329 insertions）：
  - 任务#8: src/api/client.ts 统一 fetch 封装; src/hooks/useVisiblePolling.ts 可见性感知轮询; src/modules/registry.ts 模块注册机制
  - 任务#9: src/pages/DashboardPage.tsx Grid 卡片墙; App.tsx 路由 / -> 工作台, /chat -> 聊天; Sidebar 加工作台入口; Header 支持 isDashboard
  - 任务#10: 6 个模块组件（OngoingModule/TodoModule/NewsModule/CountdownModule/LinksModule/FocusModule）
  - 任务#11: ChatPage.tsx 无 API Key 时显示配置引导卡片，SDK 代码不动
- Vite dev 验证：全部 13 个前端文件 HTTP 200 加载成功
- .gitignore 更新：排除 tsc 编译产物（.js/.d.ts/.tsbuildinfo）
- 已 push 到 GitHub (478ca2b)
